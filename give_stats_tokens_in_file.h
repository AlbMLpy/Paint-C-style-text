#include <stdio.h>

/**
 * Input parameter:
 *    - "fd" is pointer in previously opened file;
 * Description:
 *    This function take file via fd and print number of tokens of different types on the screen according to 
 *    rules of C language;
 */
void
give_number_tokens(FILE *fd);
