#include <stdio.h>

/**
 * Input parameter:
 *    - "fd" is pointer in previously opened file;
 * Description:
 *    This function take file via fd and print colored token on the screen according to 
 *    rules of C language;
 */
void
print_colored_tokens(FILE *fd);
